package AMCAT_PREP.amcat_prev;

public class 5 {
    /**6. Maximum Subarray Sum
Given an array of integers (which may contain negative numbers), find the maximum sum of a contiguous subarray.

At least one element must be included in the subarray.

Input:
Integer N followed by N integers.

Output:
Maximum subarray sum.
 */
public static void main(String[] args) {
    int[] nums={-2,1,-3,4,-1,2,1,-5,4};
    int n=nums.length;
    int maxcurrent=nums[0];
    int



}
