package Bit_manipulation;

public class bit_masking {
    /**
     * Basic operations:
1️ Set bit (1 karna)
mask = mask | (1 << i)

2️ Check bit (bit i 1 hai?)
(mask & (1 << i)) != 0

3️ Unset bit (0 karna)
mask = mask & ~(1 << i)

4️ Toggle bit (on/off flip)
mask = mask ^ (1 << i)
     */
    
}
